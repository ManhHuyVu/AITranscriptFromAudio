spring.application.name=AITranscript
spring.threads.virtual.enabled=true
# Allow TomCat (Web server) to use virual thread instead of the 200 thread limit

# Other command to look out for:

# server.tomcat.threads.max to something like 300
# That still uses platform threads, still works, still satisfies the same requirement line.
# The difference is memory cost per thread. 